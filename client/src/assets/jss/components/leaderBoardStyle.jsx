import {
  drawerWidth,
  drawerCloseWidth,
  transition,
  boxShadow,
  defaultFont,
  primaryColor,
  primaryBoxShadow,
  defaultBoxShadow,
  grayColor,
  infoColor,
  successColor,
  warningColor,
  dangerColor,
} from "assets/jss/baseStyles.jsx";

const leaderBoardStyle = (theme) => ({
  container: {
    display: "block",
    height: "100%",
  },
});

export default leaderBoardStyle;
